package devchats.client

/**
 * Менеджер подключений DevChats — клиентская часть.
 *
 * В M2 сюда добавится Ktor-клиент: установка соединений с узлами по
 * `IP:port`, рукопожатие [devchats.protocol.Hello], состояние подключений,
 * очередь исходящих сообщений и синхронизация почтового ящика.
 */
object ConnectionManager {
    // заглушка: логика появится в M2
}
